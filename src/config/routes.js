import SplashScreen from '../screens/SplashScreen';
import MainNavigator from '../mainNavigator';

const routes = {
  splash: { name: 'splash', screen: SplashScreen },
  mainFlow: { name: 'mainFlow', screen: MainNavigator },
};

export default routes;
